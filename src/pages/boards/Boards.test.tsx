import { render, screen } from '@testing-library/react';
import React from 'react';
import Boards from './Boards';

describe('<Baords />', () => {
  it('should be ', () => {
    render(<Boards />);
  });
});
