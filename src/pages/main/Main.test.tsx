import { render, screen } from '@testing-library/react';
import React from 'react';
import Main from './Main';

describe('<Main />', () => {
  it('should be ', () => {
    render(<Main />);
  });
});
