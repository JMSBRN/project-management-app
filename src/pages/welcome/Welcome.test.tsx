import { render, screen } from "@testing-library/react";
import React from "react";
import Welcome from "./Welcome";

describe("<Baords />", () => {
  it("should be ", () => {
    render(<Welcome />);
  });
});
