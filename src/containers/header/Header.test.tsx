import { render, screen } from "@testing-library/react";
import React from "react";
import Header from "./Header";

describe("<About />", () => {
  it("should be ", () => {
    render(<Header />);
  });
});
