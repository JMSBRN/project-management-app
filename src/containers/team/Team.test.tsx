import { render, screen } from "@testing-library/react";
import React from "react";
import Team from "./Team";

describe("<Team />", () => {
  it("should be ", () => {
    render(<Team />);
  });
});
