import { render } from '@testing-library/react';
import React from 'react';
import Footer from './Footer';

describe('<Header />', () => {
  it('should be ', () => {
    render(<Footer />);
  });
});
